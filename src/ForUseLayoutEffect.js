import React,{useLayoutEffect} from 'react'

function ForUseLayoutEffect() {
    useLayoutEffect(() => {
        effect
        return () => {
            cleanup
        };
    }, [input])
    return (
        <div>
            <h2>use layout effect practice</h2>
            <hr />
            // the only diffrence between use effect and use layout effect hook is that always use layout effect campe
            {/* prior to the use effect
             */}
        </div>
    )
}

export default ForUseLayoutEffect
